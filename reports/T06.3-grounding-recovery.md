# T06.3：逐条 Grounding、部分保留与仅模型复验

执行日期：2026-09-26。本轮代码与离线验证完成；唯一真实模型复验在读取响应时超时，**真实接纳 Evidence=0，G1 仍 FAIL**。没有新增小红书访问，没有继续新功能开发。

## 基线与范围

- 起点与远端均为 `62bb3aa5ab87b960712d396e1676bfbe394e87e7`，沿用 `feature/g1-live-llm-validation`。
- 已完整阅读 AGENTS.md、CODEX_START.md、当前任务、验收规格及 T06.2 报告/恢复设计和相关实现。
- Chromium 调试任务未唤醒；登录、浏览器、供应商配置、地图、酒店、报价、GUI 未修改。
- 三份原有未跟踪 T03 报告保留，不纳入提交。真实 SQLite、源缓存和 profile 均未提交。
- 代码修复先提交为 **`8be471323588f3cf3a01518fab79631d5f19131e`**，随后才调用模型，原账本记录该完整 SHA。

## 已修复的行为与契约

T06.2 `rejected_claims == 0` 是明确的整批接纳门槛，导致一个失败候选阻止所有其他候选入库。本轮显式替代这项规格：独立合格结果可以保留，但必须分别经过定位和上下文审核。

| 文件/符号 | 变更 |
|---|---|
| `grounding.py::GroundingResult/check_grounding/context_hazard` | 逐条枚举原因、候选索引、原块号与规则 v2；保持 claim==quote、逐字条件、精确坐标，捕获明显否定截断 |
| `extractor.py::_grounded/build_claim/ExtractionResult` | 复用严格定位与 Evidence 构造；安全摘要不含候选文字；敏感污染继续整批失败 |
| `recovery.py::ExtractionRecovery` | 候选入私人审计区；PENDING_REVIEW/PARTIAL_SUCCESS/NO_ACCEPTED_EVIDENCE；跨版本保留原批次额度 |
| `candidate_review.py::review_candidates` | 八项 Work 上下文核对、依赖隔离、必要原文条件锚点；证据/来源/候选/attempt 原子提交 |
| `store.py::save_evidence/_summary` | 幂等合并已审核证据，独立失败不清掉旧合格结果；持久化状态及分项计数 |
| `service.py`、`retry.py` | 部分成功不再统一当整批错误；待审候选不计 Coverage；报告失败不回滚已提交证据 |
| `models.py`、`quality.py`、`reporting.py` | 发布计数与候选计数分离；待审不得支持结论；新增条件适配尚未确立的提示 |
| `retry_extraction.py` | 仅现有 DeepSeek HTTPS 的模型入口；标准输入本地审核入口完全禁网 |
| `private_cache_probe.py` | 在已有缓存验证 revision 上递增，保留原问题历史；空证据不能通过恢复门槛 |

SQLite v6 → v7：迁移原 extraction_attempts 的失败记录，补 content_hash/提取模式并扩展状态；新增 extraction_candidates。尝试唯一性按批次、内容、次数绑定，不随 extraction_version 重新获得首次额度。domain schema 新增 GroundingCandidateCheck/GroundingCounts，扩展 ExtractionAttempt/ClaimAssessment；OpenAPI 仅同步内部契约说明，未新增公开接口。完整设计见 [T06.3 契约](../docs/architecture/t063-grounding-recovery.md)。

私人候选仅保存必要 claim/quote/conditions 和审核决定，归原快照/attempt 管理并随缓存级联删除；不保存模型响应包、reasoning、headers。原始正文仍按 PERSISTENT 保留，本轮没有删除或改成 SESSION。KnowledgeCard、清原文不清知识等设计未实现。

定位通过不等于语义成立。新提取路径须 Work 分别核对主体、否定、假设、时间、交通、范围、来源类型、图片；未确认条目保持待审或拒绝。合格数、来源数、独立来源数分开，一篇 PARTIAL_TEXT 不会变成多篇独立完整资料。此审核方式不等于用户人工验收，也不能普遍证明自动校验没有幻觉。

## 真实模型复验

环境：Windows 11、Python 3.14.7；现有 api.deepseek.com / deepseek-v4-flash，原有 30 秒超时和配置保持不变。没有连接测试或另行模型审稿。

调用前已确认：

- 本地 source_id、content_id、content_hash、全部 BodyBlocks 与原尝试一致，完整序列化内容摘要也与 T06.2 调用前检查点相同；不是仅比较长度。
- canonical 961 字符、16 块、normalization_version=1、PARTIAL_TEXT；过滤后发送原编号 16 块，共 946 字符，不含块间换行，低于 6,000 字上限。
- 原 `t062-private-live` 批次仅 1 次已用、上限 4；额外重跑尚未使用。新请求在同批次、同 content_id 记 attempt_number=2、extraction_version=2，没有新建模型批次。

实际只执行一次 `scripts/retry_extraction.py --live ... --fix-commit 8be471323588f3cf3a01518fab79631d5f19131e`：

| 诊断 | 实测 |
|---|---|
| HTTP 派发尝试 | 1 |
| HTTP 状态 | 200 |
| stage/category | TRANSPORT / TIMEOUT |
| 耗时 | 30.1953 秒 |
| finish_reason / response_model | UNKNOWN / UNKNOWN |
| 完整响应字节、content 长度 | NOT_MEASURED / UNKNOWN |
| 本次 attempt | FAILED |
| 本次候选审核 | NOT_RUN：没有取得完整响应及可解析候选 |

实现先记录 HTTP 状态，再读取完整响应，故 HTTP 200 与读取超时可以同时出现。不能据此断言模型已完整生成、JSON/schema 通过或定位失败。服务端执行/计费情况 UNKNOWN；没有保存半截响应或尝试重组候选。

原 attempt 仍为 FAILED/UNGROUNDED，12 条候选、11 条定位通过、1 条拒绝的历史诊断没有修改。历史被拒原条目未保存，**确切拒绝规则仍 UNKNOWN**；本轮超时也没有提供证据还原历史原因。T06.1 丢失的现场仍不可恢复。

本次后原批次尝试已用 2/4，额外重跑已经用完。剩余总量不等于允许再次重跑这一篇，当前同源后续重试被耐久账本禁止。没有增加超时、换模型、换服务、重试或新建批次重置额度。

## 分项真实结果

以下是**本地本次收到并登记的候选**计数；服务端实际生成多少条 UNKNOWN。零值不代表生成正确或审核通过。

| 指标 | 结果 |
|---|---:|
| generated_candidates（本地成功解析） | 0 |
| locator_passed_candidates | 0 |
| rejected_candidates | 0 |
| context_review_pending | 0 |
| persisted_evidence | 0 |
| published_important_claims | 0 |
| unsupported_published_claims | null / NOT_EVALUATED |

整批本次失败原因是 TIMEOUT，没有候选级拒绝原因可报。Work 逐条真实核对 NOT_RUN，用户人工审核 NOT_RUN；没有把历史 11 条当成已审核证据。

## 跨进程恢复与剩余 Coverage

模型进程退出后，另一个独立进程运行 `tools/private_cache_probe.py`，以 Python audit hook 禁止 DNS/connect/bind/sendto；NoAccessReader 的 connect/search/detail 计数均为 0，模型调用 0，浏览器 0。

| 检查 | 结果 |
|---|---|
| 同一原文、hash、16 个块及元数据恢复 | PASS，与 T06.2 检查点相同 |
| 原研究报告保持不变，缓存报告可恢复 | PASS |
| 旧 FAILED + 新 TIMEOUT、原批次额度跨进程保留 | PASS |
| 非空 Evidence ID/hash/条件/定位恢复 | **FAIL：实际 Evidence=0，不能空集通过** |
| 部分成功的真实恢复 | NOT_RUN：本次没有真实部分结果；合成回归通过 |
| 五天、不自驾增量 | 正确保留原始资料，新增 DAYS_FIT/NON_SELF_DRIVE 缺口；未提供可行性结论 |

恢复探针整体退出码 2、状态 FAIL，正是非空 Evidence 门槛未满足；不把原文恢复成功包装成 Evidence 恢复成功。原研究 revision 不修改；只递增既有本地缓存验证问题的 revision 2/3，该过程没有模型批次或模型额度变更。

Q1 路线、Q2 体验、Q3 时长、Q4 限制全部 UNSUPPORTED。仍缺路线、体验、时长、交通依据、不同来源对照与主题覆盖；追加条件后另缺五天适配和不自驾交通。原缓存仍为一篇 PARTIAL_TEXT，图片未分析，不能以缓存存在就认定研究充分。

可读研究结论：**本轮没有完成接纳的旅行证据，暂不能给出有审核依据的路线或时间安排。五天、不自驾仍需补充适配依据；没有硬凑方向、报价或可行性承诺。** 没有为填缺口访问第 2/3 篇。

## 离线验证

- `python -m pytest -q --tb=short`：最终 **849 passed，2 条既有依赖弃用警告**，12.94 秒。新增 19 个自编材料用例；没有外网测试。中途发现一个旧用例将敏感污染与未发送块混为一项，分离夹具后全量重跑通过。
- `python -m ruff check .`：PASS。
- `python -m mypy`：PASS，44 个源文件。
- `python tools/validate_pack.py`：12/12 PASS；46 个领域定义，schema 正反例、引用、OpenAPI 结构和文档链接通过。
- `git diff --check`：PASS。

新增回归实际覆盖：2 接纳/1 拒绝的 PARTIAL_SUCCESS；无效条件整条拒绝及依赖隔离；全部拒绝的空质量不通过；不存在引文/错块/未发送块/重复与多余块枚举；否定截断；自驾不能改成不自驾；空白/Unicode/标点/数字坐标；v6 历史迁移；跨版本/新 run/新 batch 不重置额度；服务/报告/恢复部分状态；独立进程非空 Evidence 0 网络恢复；账号与 revision；来源删除级联审计；证据提交中途回滚；报告失败已提交证据保留；本地审核 CLI 禁网；敏感响应整批失败及 sentinel 不落库。

## 访问与安全

| 项目 | 本轮 |
|---|---|
| XHS connect/search/detail | **0 / 0 / 0** |
| BrowserSession / sidecar 启动 | 0 / 0 |
| 新增模型提取 / 连接测试 / 独立审稿 | **1 / 0 / 0** |
| 仅恢复进程外部操作 | 0 |
| HTTP 请求派发计数、HTTP 状态、耗时 | 实际应用层诊断；不是抓包计数 |
| 全主机网络请求、传输字节、服务器内部重试 | NOT_MEASURED |

0 XHS 的依据是入口没有读取器/浏览器调用、XHS/Playwright 模块未导入，以及模型进程只允许现有 DeepSeek DNS/HTTPS、恢复进程完全禁网的运行守卫；不是全主机流量监测。Git 推送属于代码同步，不算模型或 XHS 访问。

已检查显式暂存清单和完整待推送提交范围：配置密钥、密钥格式、运行文件路径、二进制、缓存正文连续 32 字片段未发现泄漏。真实库和本轮安全 JSON 日志未包含配置密钥，安全日志未含正文片段。合成敏感 sentinel 拒绝用例通过；不宣称检测覆盖所有可能个人信息。

专用 profile 目录存在并保留，原始缓存保留；没有读取 Cookie/session/token。模型与缓存探针进程均正常结束。结束时只读进程检查：本次恢复辅助进程 0、使用 TravelAgent 专用 profile 的浏览器进程 0。沙箱内进程查询曾被权限限制，随后提升只读查询成功，未把初次空输出当成结果。

## 提交和停止

代码提交：[8be471323588f3cf3a01518fab79631d5f19131e](https://github.com/wenbox98/TravelAgent/commit/8be471323588f3cf3a01518fab79631d5f19131e)。代码/契约/测试/设计共 29 个文件，976 行新增、112 行删除；本报告、README 当前状态及验收文档随后另行提交。最终报告提交 SHA 与远端完整 SHA 核对结果在交付回复中给出，不把自引用占位当成提交记录。

同步状态：**PUSH_BLOCKED**。`git push origin feature/g1-live-llm-validation` 和随后一次只读 `git ls-remote` 均返回 `schannel: failed to receive handshake, SSL/TLS connection failed`。未关闭证书校验、换远端或强推。最终远端 HEAD 无法核对；任务开始时曾核对远端等于基线。本地提交仍完整保留，上面的提交链接是目标链接，尚不能视为远端已可访问的交付。

**G0 仅保留历史 PASS，本轮未复测；G1 FAIL。** 真实验证当前被响应读取超时阻断，真实部分接纳/非空恢复尚未验证。下一步若另行授权，应先审查模型响应耗时和超时策略；任何新增真实调用都需要明确调整原批次授权，不能另建批次绕过。当前不修改供应商配置、不继续请求、不进入新功能。
