# T06.5：有界补充资料与第一份可读局部攻略

执行日期：2026-09-26。**新增 1 次搜索、2 篇详情、2 次模型提取；新增接纳 3 条，连同旧 3 条共 6 条 Evidence。非空跨进程恢复和五天/不自驾零访问增量 PASS。G1 仍 NOT PASS：路线和总时长证据未通过，不能排出可信的完整粗略路线。** 已交付私人可读内容，不以测试数量替代攻略。

## 基线、修改和授权

- 分支 `feature/g1-live-llm-validation`，起点 `29e5e940909b778942edc2250315eb946fcae9c6`；开工时本地与远端完整 SHA 一致，没有待推送旧提交。
- 先完成离线修复并提交 `8320e988414d875934d784732e7b6ab71c9a2cce`，再实测。该提交 28 文件，715 行新增、57 行删除；包含明确的领域元信息/SQLite v9 契约变化，见 [设计说明](../docs/architecture/t065-coverage-closure.md)。
- 三份原未跟踪 T03 报告保留，不提交。没有切换 master、reset/rebase、强推、关闭 TLS 验证或唤醒 Chromium 调试任务。
- 用户本次固定授权绑定 `t065-coverage-first-plan`，前序研究 `t062-private-live`（含 T06.4 追加结果）；原数据库继续使用，不修改旧批次、旧失败或已消费的追加授权。

主要修改符号：

| 文件/符号 | 解决的问题 |
|---|---|
| planning.py / CandidateSelector.select、QueryPlanner.plan | 去掉标题必须包含总区域名的硬过滤；查询上下文只能是弱候选信号；局部体验之后优先查路线/行程/天数/交通 |
| reporting.py / build_directions、render_private_report | 跨源同名方向隔离；显式同源对象关联；逐条保留条件，展示未组成路线的合格局部材料；已知天数/交通不重复询问 |
| candidate_review.py / review_candidates；ClaimAssessment | 保存 Work 审核的对象原文锚点、scope 和 lineage；非路线关联必须有同源已接纳路线，不凭相邻段落拼接 |
| freshness.py / assess_freshness | 明确作者当次经历可作为历史参考，不强制精确到日；动态费用/开放/预约不能借此通过；旧元信息与旧成绩不变 |
| continuation.py；SQLite v9 | 固定且不可重置的 1 connect / 1 search / 2 detail / 2 model 账本，派发前预留，同来源去重 |
| recovery.py、retry.py、service.py、private_smoke.py | 正常新来源提取接入 120 秒等待和 180 秒自有进程监督；原文先保存，逐条审核后继续计算缺口；质量弱可读下一新来源，传输失败停止 |
| providers/llm.py | 仅增加缺口提取优先级，保持原模型、响应格式、候选 schema 和逐字标准 |
| tools/private_cache_probe.py | 独立禁网恢复，并生成首轮/追加条件的短报告；恢复范围包含同批读过但未接纳的来源快照 |

合成复现先发现跨源同名被合并，以及跨段明确行程关系不能关联。修复测试覆盖单次行程分段、两条不同天数路线、D1 活动时间不变成全程时间、跨源不混接和孤立关联拒绝。**本轮真实 ROUTE 最终为 0，所以新路线关联和 AUTHOR_RECORDED_TRIP 元信息仅通过离线回归，没有声称在本批真实路线中验证成功。**

## 缓存先查、查询和入选理由

连接前读取旧 3 条已接纳证据及其必要条件；完整 Evidence 摘要与 T06.4 检查点一致。已知内容只是局部作者体验，缺路线组合、总天数、交通与来源对照。先保存私人研究计划，再连接。旧来源没有重新读取详情或重新提取。

唯一实际查询：**`成都 川西 国庆 路线 行程 天数 交通`**。来自当前 request 与 ROUTES/DURATION/TRANSPORT 缺口，比重复泛“攻略”更针对正文行程、天数与交通；没有把用户预设成五天或自驾。

搜索返回 20 个去重候选。排序只用真实标题、图文类型、详情可读性、来源去重、查询上下文和标题差异；不使用热度排名、模型排名、封面文字或 IP 属地推断目的地。request.destination 仍是检索标签，不是地理事实证明。

本报告固定匿名编号：S1=旧来源，S2=第一篇新来源，S3=第二篇新来源。原 Smoke 的 selection 数组按“本轮入选序号”从 S1 开始；本报告和最终私人恢复输出统一为上述三来源编号，不混用。

| 来源 | 入选前实际可见信号与预期 | 读后实际发现 |
|---|---|---|
| S2 | 标题含目标区域、国庆、七天环线和电车自驾，预期补路线/时间/交通；与旧资料去重 | 是尚未出行的方案征求意见帖，不能当成作者完成行程的经历；模型候选全部逐字失败 |
| S3 | 标题含出发地、五天四晚、秋景和自驾攻略，预期补不同片区的行程及体验；标题差异较大 | 攻略整理，未确认作者亲历；部分局部描述可用，标题天数不能替代未通过的正文时长证据 |

标题出现“自驾/五天”只用于评估材料潜力，没有改写首轮用户条件。第一篇传输正常但质量不合格后，按剩余额度处理另一新来源；没有回读或重试第一篇。

## 实际额度、来源与传输

| 操作 | 实际新增 | 上限 | 剩余 |
|---|---:|---:|---:|
| connect / BrowserSession | 1 / 1 | 1 / 1 | 0 |
| search | 1 | 1 | 0 |
| detail / 新来源 | 2 / 2 | 2 / 2 | 0 |
| 模型 HTTP 派发尝试 | 2 | 2 | 0 |
| 连通性测试、模型排名/审稿/报告生成、自动重试 | 全部 0 | 0 | 0 |

两篇详情均通过 SourceIdentity 校验、主响应 200，正文/块先入库。S2 原始正文 707 字符，canonical 705 / 21 块，外发 685 字符；S3 原始正文 1,076，canonical 1,048 / 36 块，外发 1,013 字符。外发计数不包含块间换行，均低于 6,000 字/120 块；原过滤保留。两篇均 PARTIAL_TEXT，分别 2/16 张图片未分析；没有因为取得文字而声称全文或图片已读。

原 `t062-private-live` 普通上限仍为 4、已用普通尝试仍为 2；原单次重跑与 T06.4 补充授权均未恢复。逐行摘要确认旧 attempts、batches、authorizations、candidates、SourceContent、BodyBlocks、claims、sources 全部原样保留。新批次只使用新来源；不是将旧来源放进新批次重试。

| 实测字段 | S2 | S3 |
|---|---:|---:|
| HTTP 状态 / finish_reason | 200 / stop | 200 / stop |
| headers 到达时点 | 0.1839 秒 | 0.1541 秒 |
| 完整正文到达时点 | 29.0579 秒 | 49.6554 秒 |
| provider 耗时 | 29.0603 秒 | 49.6581 秒 |
| 监督范围耗时 | 29.3681 秒 | 49.9410 秒 |
| response_bytes / content_chars | 30,496 / 3,872 | 52,320 / 3,936 |
| 生效 timeout / 总截止 | 120 / 180 秒 | 120 / 180 秒 |
| 超时 / 额外重试 / worker 遗留 | 0 / 0 / 0 | 0 / 0 / 0 |

接收方仍为 `api.deepseek.com`，requested_model=`deepseek-v4-flash`，response_model=`deepseek-flash`（仅记录服务端回报），响应格式 `json_object`。没有改变供应商或模型，没有完整响应包/reasoning/headers 落入普通日志。第二篇耗时超过 30 秒，不据此断言原 30 秒设置必然失败：本次未单独测量保活与网络分段。

## 逐条真实接纳

| 计数 | 旧 S1（保留） | 新 S2 | 新 S3 | 本轮新增合计 |
|---|---:|---:|---:|---:|
| 候选 | 12 | 12 | 12 | 24 |
| 定位通过 | 11 | 0 | 6 | 6 |
| 最终接纳 | 3 | 0 | 3 | **3** |
| 最终拒绝 | 9 | 12 | 9 | **21** |
| 待审 | 0 | 0 | 0 | 0 |

S2 C0–C11：CLAIM_QUOTE_MISMATCH，12 条；模型改写/翻译了引文，即使 quote 看似存在也不能自动修成通过。

S3（编号为 0-based）：

| 候选 | 结果 | 原因与范围 |
|---|---|---|
| C0–C4、C9 | 拒绝，6 条 | CONDITION_NOT_IN_CITED_BLOCK：条件引用 block 0，但 source_block_ids 未列入该块。实际并非“条件文字不存在”，而是引用集合不完整；未自动补齐后接纳 |
| C5 | 拒绝 | UNVERIFIED_IMPORTANT_FACT：当前景区接驳指令，缺亲历时间/当前运行依据，不作为可执行交通建议 |
| C7 | 拒绝 | UNVERIFIED_IMPORTANT_FACT：对整个区域、整段月份的概括，不能承诺当期景观表现 |
| C8 | 拒绝 | UNVERIFIED_IMPORTANT_FACT：带绝对判断的高反与住宿指令，不转成医学/安全建议 |
| C6 | 接纳 | 仅是原自驾攻略的返程安排线索；补充季节、交通、对应日段条件；当前适用性仍未核实 |
| C10、C11 | 接纳 | 两项独立局部景观描述；保留秋日、晴空/徒步、对应片段、自驾攻略条件；不声称已确认作者亲历或当前观景保证 |

新增拒绝总计：CLAIM_QUOTE_MISMATCH=12、CONDITION_NOT_IN_CITED_BLOCK=6、UNVERIFIED_IMPORTANT_FACT=3。Work 实际查看新来源必要正文及全部候选，6 条定位通过的记录均有明确上下文决定；不是用户手工逐条审核，也不是产品已能自动审核。

S3 接纳时保留原条件，并加入原文逐字锚点。借用原文的季节/日段仅说明描述适用范围，不将被拒绝的整条路线/总时长复活；记录 INDEPENDENT 说明独立成立。没有使用 AUTHOR_RECORDED_TRIP，因为本篇未确认亲历。所有已接纳条目保持 AUTHOR_OPINION / MEDIUM / PARTIAL_TEXT。旧 S1 的拒绝记录完全不动。

## 真实可读内容与方向字段

私人目录 `.local/t06.5-live/` 已生成：

- `rough-plan.md`：第一轮可读局部攻略，两组兴趣线索、差异、条件、时长未知和两个选择问题。
- `first-plan.md`：程序渲染的 6 条短引文、逐条条件和统一 S1/S2/S3 来源表。
- `five-days-summary.md`：五天、不自驾的可读增量说明。
- `five-days-no-driving.md`：独立禁网恢复生成，保留同一组 6 条和来源条件，不再询问已知天数/交通。

可读内容只概述已接纳材料。A 组是旧作者某次自驾关注的地点与个人比较，明确包含一个作者实际未游览的条件建议；B 组是新攻略的秋季山湖描写及带限制的返程线索。**两组是兴趣线索，不是已验证的两条路线，也没有完成多方案时长比较。** 真实地点与必要摘录只在私人输出，不提交全文/真实数据集。

| 私人展示组 | 路线字段 | 体验依据 | 时间字段 | 交通/条件依据 |
|---|---|---|---|---|
| A：旧来源局部兴趣 | 无已接纳 ROUTE | S1 C7/C8；有条件建议 C5 | 已审核年份/国庆背景；没有整趟或活动时长 | 来源自驾条件；C5 明确未实际游览 |
| B：新来源山湖兴趣 | 无已接纳 ROUTE | S3 C10/C11 | 秋季描述与日段，只作范围；无合格整趟时长 | S3 C6 返程线索 + 自驾/季节条件，CURRENT_UNVERIFIED |

没有用 A 的时长或 B 的交通填充另一组。机器正式 candidate_direction_count=0，路线发布计数=0；局部展示有 6 条可回溯材料，不将“没有发布错误路线”当成 G1 通过。

## 跨进程恢复、增量与 Coverage

进程 A 先保存 6 条 Evidence 的完整对象摘要和 ID；独立进程 B 通过 socket 审计钩子拒绝全部网络，再恢复。完整摘要一致，13 项检查全部 true，定位审计 checked=6、unsupported=0。恢复 **3 个来源快照、73 个正文块**，包含没有接纳证据的 S2；不会把两个空列表相等算作成功。

恢复和五天/不自驾增量：connect/search/detail/model/browser 全部 **0/0/0/0/0**。保留 6 条、条件、来源与 lineage、原报告和原文；仅增加用户偏好，不修改来源经历。私人增量明确无法给出可信五天非自驾路线，不编车次、接驳和费用。

| 维度 | 结果 |
|---|---|
| Q1 路线 | UNSUPPORTED，0 条 |
| Q2 体验 | PARTIAL，5 条，尚未关联到合格路线 |
| Q3 时长 | UNSUPPORTED，0 条 |
| Q4 交通/限制 | PARTIAL，1 条且当前适用性未核实 |
| 来源 | 2 个有证据来源簇，独立性 UNKNOWN；confirmed independent=0 |

剩余基础缺口：ROUTES、EXPERIENCES、DURATION、TRANSPORT、CLAIM_DIVERSITY；另有部分文字/未分析图片/正文表示关系/旅行时间信息不足。增量额外保留 DAYS_FIT、NON_SELF_DRIVE。没有降低 G1 阈值；也没有把粗略研究门槛提高到未来完整报价/预订验收。

G0 保留历史 PASS；本轮没有重测完整 G0。T06.4 完整响应、部分保留、非空恢复 PASS 不回退。T06.5 传输、限额、部分保留、非空恢复 PASS；**G1 NOT PASS**，主要是模型候选违反既有定位契约，且来源缺少已审核路线与时长，不是传输又超时。

## 网络、浏览器与安全

Windows 11 / Python 3.14.7 / Playwright 1.63.0，managed Chromium 153.0.8010.12（chromium-1243）。沿用普通 persistent context：channel=None、headless=False、args=[]、no_viewport=True、chromium_sandbox=True，专用 profile；不加入伪装、代理参数。

已有 profile 直接 AUTHENTICATED，无再次扫码要求；一个 BrowserSession。正常关闭，未 disconnect，profile 和私人库保留。退出后只读进程检查：专用 profile 浏览器=0，研究/模型辅助进程=0；没有独立启动 HTTP sidecar 服务。

| 观测窗口 | context request events | 主 frame navigation request events |
|---|---:|---:|
| LOGIN | 194 | 1 |
| SEARCH | 167 | 3 |
| DETAIL_1 | 172 | 1 |
| DETAIL_2 | 160 | 1 |
| OUTSIDE_WINDOW | 68 | 0 |
| TOTAL | **761** | **6** |

搜索窗口观测两个 301 响应；一次业务 search 不能当成一次 navigation。总体 document=6、xhr/fetch=301、image=258、stylesheet=42、script=140、other=14；finished=724、failed=37。累计观察约 249.393 秒，包含模型等待与 Work 复核期间页面自身的后台活动。

默认 OBSERVE_ONLY，没有主动下载/保存图片或视频，正常浏览器仍加载图片资源；没有展开评论，但页面默认评论相关事件 DERIVED=2。没有点击发布、点赞、收藏、评论、私信等写操作；浏览器正常页面的 HTTP POST 不等于执行这些平台内容写功能。没有 stealth、fingerprint、proxy、验证码绕过或轮换。

这些仅是 `context_events_since_attach` 观测，不是全部浏览器/主机 wire requests。实际发送请求、传输字节及服务端内部行为仍 NOT_MEASURED，不能宣称本次证明了网络节省率。

登录审计 2 行，unrecognized=0、sentinel_matches=0。配置密钥及敏感访问材料扫描覆盖本轮安全摘要/私人输出和完整待推送范围；真实源码块连续 32 字片段另行检查，运行库、profile、图片、原文不进入 Git。未发现泄漏；这不是对全系统隐私的无限保证。

## 离线验证与实测后的局部修正

实测前：872 项测试 PASS，Ruff PASS、Mypy PASS（45 文件）、文档/契约 12/12 PASS（47 定义）、git diff --check PASS。最初失败用例分别复现跨源合并，以及更新迁移版本后旧测试仍断言 v8；均解决后才运行实测。曾误将 Mypy 检查范围扩大到整个历史 apps/api，产生已有未注解模块错误；最终按仓库配置运行，不隐藏这次失败，也没有为通过而放宽配置。

实测后发现顶层 diagnostic 残留 S2 的 NO_ACCEPTED_EVIDENCE，尽管后续 S3 已部分成功。原实测摘要保留；仅离线修正成功后清除过期顶层提示，并保留逐篇诊断。新增回归验证第一篇无接纳、第二篇成功的场景。恢复工具同时覆盖同研究所有 run 的来源快照，避免只取最后 Work 审核 run 而漏掉 S2；短报告逐条补充 CURRENT_UNVERIFIED。没有重新进行真实测试或额外模型调用。

最终执行 `python -m pytest -q --tb=short`：**873 passed**，27.15 秒，两个既有依赖弃用警告；`python -m ruff check .` PASS；按配置执行 `python -m mypy` PASS（45 文件）；`python tools/validate_pack.py` 12/12 PASS；`git diff --check` PASS。以上均为离线验证，没有新增真实请求。

收尾独立禁网审计再次确认：6 条完整 Evidence 摘要与恢复前后相同；8 张表的全部旧行仍原样存在；新账本实际为 CONNECT=1、SEARCH=1、DETAIL=2、MODEL=2。对 13 个私人输出/检查点/数据库文件，以本机配置密钥实际值及敏感访问字段模式扫描，发现数 0；未打印密钥、Cookie 或 token。完整待推送范围另行扫描，结果与远端完整 SHA 核对在本轮交付记录中给出。

本轮相对基线的 `git diff --stat`：**31 files changed, 907 insertions(+), 60 deletions(-)**。范围为候选/条件/明确关联修复、固定额度与提取监督接入、恢复工具、合成测试、契约和匿名报告；私人研究产物及三份历史 T03 报告均不在差异中。

## 新问题、待办和停止

1. 标题可见的天数/路线仍不能识别是已完成游记还是待出行方案；本批 S2 即反例。
2. 模型在 24 条中产生 12 条 claim/quote 不一致、6 条条件引用集合不完整。要解决提取契约可靠性，不能通过自动补引文/抹条件让 G1 变绿；本批不再请求模型。
3. 明确跨段关系已有离线能力，但真实路线全被定位规则拒绝，不能声称该能力已补齐本批 Coverage。
4. WORK_REVIEWED 依赖开发 Work，不等于无人值守产品审核；需后续定义自动审核与待审边界。本轮未引入多 Agent 审稿。
5. KnowledgeCard、原文清理和知识库生命周期继续留为设计待办。本轮不删除原文，也不进入地图、酒店、报价、GUI 或新产品功能。

本批剩余额度均为 0，正常关闭并停止。后续如继续，应先解决已缓存候选的提取契约问题；任何新增真实请求需新的明确范围，不能重置本批账本。
