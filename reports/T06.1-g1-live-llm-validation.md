# T06.1：真实 LLM 与真实资料 G1 验收

## 当前进度：等待模型配置

**G0 = PASS（沿用已接受结果）；G1 = G1_LIVE_LLM_BLOCKED。** 本文件目前仅记录启动预检，不是已完成的真实验收报告；配置完成后继续更新实际结果。

已完整阅读本轮用户要求和根 AGENTS.md、CODEX_START.md、README.md。通过 `git fetch origin master` 确认 master 与 origin/master 均为 `f3a14a25bf0b8e013acac5d15e964cd20c0d5487`。已从该基线创建 `feature/g1-live-llm-validation`，未直接在 master 修改。

Process/User/Machine 三个环境范围内，`LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL` 的存在性均为 false。仅记录布尔结果，没有输出或收集密钥。项目原先缺少 `.env.example`，现已补充空值参考和 README 配置步骤；未创建真实 `.env`，未修改 provider 或业务代码。

## 预检状态

| 项目 | 本轮结果 |
|---|---|
| provider 类型 | 沿用 OpenAICompatibleProvider；未发请求 |
| model | 未配置 |
| 模型连通与真实 structured output | NOT_RUN，等待配置 |
| XHS connect / search / detail | 0 / 0 / 0 |
| 实际读取 source / 新 Evidence | 0 / 0 |
| 真实 locator coverage / unsupported / conflict / coverage | NOT_MEASURED |
| 真实旅行方向输出 | NOT_RUN，不用合成资料冒充 |
| 真实跨进程缓存 / 五天不自驾增量 | NOT_RUN |
| 本轮业务代码测试 | 未重跑；T06 基线为 722 PASS，真实验收前必须重跑全部 |
| 配置文档检查 | `python tools/validate_pack.py` 12/12 PASS；184 个相对链接有效 |
| 本地预检 | `scripts/research_quality.py live-preflight` 实际返回 G1_LIVE_LLM_BLOCKED / LLM_NOT_CONFIGURED，live_operations=0 |

原有三个未跟踪 T03 报告保持原样，不纳入本轮提交。没有触碰登录 profile、Cookie、token、真实正文或 SQLite。没有推送新分支。

本次仅提交配置参考、README、预检报告及生成的文档校验记录；`git diff --cached --stat`：5 files changed，74 insertions，4 deletions；`git diff --cached --check` PASS。

## 下一步

请在本机配置上述三个环境变量，并重启负责运行验收的进程，使配置生效。按照本轮用户第 2 节的明确要求，当前暂停；配置完成后先检查全部离线测试和完全合成 BodyBlock 的真实模型连通性，再按来源用途约束推进最多 1 search / 3 detail 的真实验收。

`T06.1-live-travel-research-example.md` 要在取得允许使用的真实证据后生成；目前没有可以据实填写的旅行方向。没有进入地图、酒店、报价或完整行程。
