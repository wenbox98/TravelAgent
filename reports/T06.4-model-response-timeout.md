# T06.4：响应等待修正与缓存正文单次复验

执行日期：2026-09-26。**本轮传输完成、逐条部分接纳、非空 Evidence 入库及跨进程零访问恢复均通过；G1 整体仍 NOT PASS，原因是研究覆盖不足。** 本轮没有访问小红书，没有继续开发产品功能。

## 分层结果

| 层次 | 结果 | 证据与边界 |
|---|---|---|
| 传输/完整响应 | PASS | HTTP 200，finish_reason=stop，完整响应 28,096 字节 |
| JSON / schema | PASS | 完整内容可解析，schema_errors=[]；不是仅收到头部或空白就算成功 |
| 逐字定位 | PARTIAL | 新生成 12 条，11 条定位通过，1 条 claim/quote 不一致 |
| Work 上下文审核 | 完成 | 11 条待审逐条处理；接纳 3、上下文拒绝 8，另保留程序定位拒绝 1 |
| 独立合格结果保留 | PASS | PARTIAL_SUCCESS；3 条非空 Evidence 原子入库，0 待审，其他条目失败没有清掉这 3 条 |
| 跨进程零访问恢复 | PASS | 3 条 Evidence 的完整对象摘要、ID、正文定位、条件、审核状态与 Coverage 一致 |
| 五天、不自驾 | 缺口正确保留 | 原 3 条及来源条件保留，新增 DAYS_FIT / NON_SELF_DRIVE，不产生可行性保证 |
| G0 | 历史 PASS | 本轮未重测站点或浏览器 |
| G1 | **NOT PASS** | 一篇局部作者观点，路线/时长/交通依据不足，不能发布完整攻略 |

## 起点及实现范围

起点 `feature/g1-live-llm-validation` / `42784639e0d53a504f03fa03fe5319a5cf92e9f7`，确认同时包含 `8be471323588f3cf3a01518fab79631d5f19131e`。工作区仅有预期的三份未跟踪 T03 报告，均保留且不提交。已读取实际本地规则、报告、provider/diagnostic/extractor/recovery/content_store/retry 与相关契约和测试。

代码修复先提交：**`c167db2f0edb68136e82940d1afd8d6ce20edec1`**，再登记授权、再执行真实请求。没有更换模型、供应商或凭据；没有修改提示词、响应格式、thinking/stream、候选上限、切块、过滤或 grounding v2 标准。没有新 SDK、KnowledgeCard、地图、酒店、报价、GUI、浏览器或登录逻辑。

主要修改：

- `providers/llm.py`、`diagnostics.py`：仅增加安全阶段检查点、实际 timeout、响应头和正文完成相对时点；请求正文构造不变。
- `research/retry.py`：复用现有模块增加固定单次追加授权的登记/消费及受控工作进程监督；复用原报告保存入口。
- `research/recovery.py`：耐久预留与单次 RUNNING 认领分开，原定位/上下文/候选保留流程不改；原普通批次计数排除独立追加授权。
- `scripts/retry_extraction.py`：本地 `--grant-extra`、一次 `--live --extra-authorization`；后者固定 180 秒总时限并验证本次 provider.timeout=120，仍只允许 api.deepseek.com。
- SQLite v8：增加 extraction_authorizations；第三次尝试必须关联唯一授权，保留原两条尝试、候选/证据及外键。domain/database/OpenAPI 显式同步。
- 新增本地 HTTP 故障注入、配置传递及 v7→v8 非空审核结果保留测试；[现有恢复设计](../docs/architecture/t063-grounding-recovery.md) 追加 T06.4 说明，无独立框架。

## 实际等待设置与观测

运行环境沿用 Windows 11、Python 3.14.7。调用前实际 provider.timeout=30.0，来源是代码默认值；LLM_TIMEOUT_SECONDS 与 TRAVEL 别名均未覆盖。配置加载优先顺序为 LLM_TIMEOUT_SECONDS → TRAVEL_LLM_TIMEOUT_SECONDS → 默认 30。仅本轮命令进程设 LLM_TIMEOUT_SECONDS=120；监督进程向自有子进程明确传递同值，没有修改机器全局设置或其他任务默认值。

urllib timeout 用于阻塞操作，不能当作整次请求总截止，见 [Python 官方说明](https://docs.python.org/3/library/urllib.request.html)。[DeepSeek 官方说明](https://api-docs.deepseek.com/quick_start/rate_limit/) 提到非流式等待可能返回空行；本轮没有逐块观测保活，不能声称实际发生排队或空白保活。

| 字段 | 实际值 |
|---|---|
| 接收方 | 原 api.deepseek.com |
| requested_model | deepseek-v4-flash，未更改 |
| response_model | deepseek-flash；服务端实际回报值，不推断内部映射 |
| response_format | 原 json_object |
| 生效阻塞等待 | **120.0 秒**，子进程诊断确认 |
| 外层总时限 | **180 秒**，本轮未触发 |
| 响应头取得时点 | 0.2161 秒，相对 provider 开始，实际记录 |
| 完整正文取得时点 | 23.2713 秒，相对 provider 开始，实际记录 |
| provider 总耗时 | 23.2736 秒 |
| 监督范围总耗时 | 23.5577 秒 |
| HTTP 状态 / finish_reason | 200 / stop |
| response_bytes / content_chars | 28,096 / 4,328 |
| transport_phase / 失败 phase | COMPLETE / 无传输失败 |
| 本轮 HTTP 派发 / 自动重试 | **1 / 0** |
| 工作进程退出码 / 遗留工作进程 | 0 / 0 |

本次耗时少于 30 秒，**不能由这一次成功反推“120 秒设置已证实解决历史超时根因”**。延长等待的有效性由离线延迟对照确认；历史请求为何等待更久仍 UNKNOWN。HTTP 200 本身不等于 JSON 或研究质量通过。

受控子进程是本轮唯一网络执行单元。离线测试验证：持续空白导致读取长期不返回时，总截止终止并等待该进程退出；再读 SQLite，仅把未完成记录标记终止，已原子提交的结果不会覆盖成失败。保留 524,288 字节响应上限，不修改 JSON 字符串，不拼接半截结果。

## 来源与追加授权账本

调用前后均验证 source_id/content_id/content_hash、全部正文块和规范化版本；完整 SourceContent 序列化摘要与 T06.2 原检查点相同。仍为原缓存一篇，canonical 961 字符、16 块、normalization_version=1、PARTIAL_TEXT，18 张图片未分析。实际按原规则发出 16 块、946 字符（不计块间换行），低于 6,000 字/120 块上限。没有补取 locator、正文或图片。

| 记录 | 本轮结束状态 |
|---|---|
| 原批次 t062-private-live 上限 | 4，未修改 |
| 原普通尝试计数 | 2，未修改；原一次额外重跑仍耗尽 |
| 原第 1 次 | FAILED / UNGROUNDED，历史计数原样保留 |
| 原第 2 次 | FAILED / TIMEOUT，30.1953 秒记录原样保留 |
| 新固定追加授权 | t064-response-timeout-once，已消费 1/1 |
| 新第 3 次 | 同 batch/content/hash，独立授权关联，PARTIAL_SUCCESS |
| 此快照累计 HTTP 派发 | 原 2 + 新 1 = **3**；不是整个项目历史总请求量 |

登记授权是单独禁网步骤；绑定原耗尽 attempt、账号/来源/快照、修复 SHA、接收方、原模型/响应格式、120/180 秒配置和用途。消费时间先耐久提交，再启动工作进程；派发检查点另行记录。固定 ID、不可变配置、唯一约束及事务保证重复/并发不能多消费。没有新建模型批次、删除失败记录或恢复旧额度。超时同样消费授权，不推断是否计费；本轮授权现在也已耗尽。

## 本次候选的实际审核

| 指标 | 数量 |
|---|---:|
| generated_candidates | 12 |
| locator_passed_candidates | 11 |
| context_reviewed（明确 Work 审核决定） | 11 |
| accepted / persisted_evidence | **3 / 3** |
| rejected_candidates | **9** |
| context_review_pending | 0 |
| 程序路线输出 published_important_claims | 0 |
| 程序路线输出 unsupported_published_claims | null / NOT_EVALUATED：没有路线输出 |

Work 查阅了本地新候选及对应原块，并逐项核对主体、否定、假设、时间、交通、范围、来源类型和图片。接纳 C5/C7/C8 时添加原文逐字可定位的年份、自驾与必要对象条件，不改 claim/quote，不删除原条件。均保持 AUTHOR_OPINION、PARTIAL_TEXT、MEDIUM；未宣称用户人工验收或多来源事实核实。

候选编号为原 0-based 索引。公共报告只记规则及摘要，必要候选引文/条件仍在受管私人 SQLite，不保存完整响应包、reasoning 或 headers。

| 候选 | 结果/原因 | 审核说明 |
|---|---|---|
| C0 | CLAIM_QUOTE_MISMATCH | claim 去掉了 quote 中的主观限定，严格相等校验拒绝；不自动修成通过 |
| C1 | UNVERIFIED_IMPORTANT_FACT | 节假日收费条件和节省金额未经核实，不形成费用依据 |
| C2 | CONTEXT_UNCERTAIN | 消费口径和指代范围不足，不用片段推出旅行预算 |
| C3 | CONTEXT_UNCERTAIN | 夸张描述不能作为可核实的体验事实 |
| C4、C6 | UNVERIFIED_IMPORTANT_FACT | 单作者“名气”评价不能证明客观知名程度 |
| C5 | WORK_CONTEXT_VERIFIED | 仅保留作者有时间才考虑的建议；必须带上作者出发较晚、停车不便、实际未游览的原文条件 |
| C7 | WORK_CONTEXT_VERIFIED | 仅保留作者自己行程的兴趣重点，补充年份、自驾和对应路段对象 |
| C8 | WORK_CONTEXT_VERIFIED | 主观景点比较，保留第一人称评价及对象；不接纳相邻的未经核实“最大”等断言 |
| C9 | CONTEXT_UNCERTAIN | 短风险提醒缺乏充分地点/适用性说明，未转成旅行安全或医疗结论 |
| C10 | UNVERIFIED_IMPORTANT_FACT | 绝对拥堵条件与最优路线判断未证实，整条拒绝，不能删条件后接纳 |
| C11 | DEPENDENCY_UNRESOLVED | 比较对象/适用范围与同块被拒路线判断的关系未厘清，保持隔离 |

原因计数：CLAIM_QUOTE_MISMATCH=1；UNVERIFIED_IMPORTANT_FACT=4；CONTEXT_UNCERTAIN=3；DEPENDENCY_UNRESOLVED=1。相同段落中的可独立主观陈述保留必要条件后可以接纳，不因其他判断未证实而整批清空。

旧 Diagnostic 的 accepted_claims=11 仍表示**逐字定位通过数**，rejected_claims=1 表示程序定位拒绝；最终审核以 extraction_results 的 persisted_evidence=3、rejected_candidates=9 为准。GROUNDING/UNGROUNDED 是定位阶段发现一个错误的诊断，顶层 PARTIAL_SUCCESS 正确保留后续审核合格结果。

历史 T06.2 同样“11 条逐字通过”的输出未保存，不能恢复或认定就是本轮这些候选；历史那条确切拒绝原因仍 UNKNOWN。T06.1 丢失现场同样没有恢复。

## 非空恢复、局部结果与 Coverage

先在一个本地进程记录 3 条已存 Evidence 的 ID 和完整对象摘要，再在另一个进程运行 `tools/private_cache_probe.py`。比对实际证据内容、条件、locator、Work 审核元信息和源 lineage 全部一致；定位复核 checked=3、unsupported=0。原 SourceContent/BodyBlocks 和原研究报告不变，来源始终 1 篇，不把 3 条当成 3 个独立来源。

缓存探针 PASS，13 项检查均 true。审计钩子禁止网络，NoAccessReader connect/search/detail=0/0/0，model=0，browser=0。相同需求和“五天、不自驾”零预算增量均保留 3 条。增量仅更新既有本地缓存验证问题 revision 4/5，未建立模型批次或增加模型调用。

| 研究维度 | Coverage |
|---|---|
| Q1 路线 | UNSUPPORTED |
| Q2 体验 | PARTIAL，3 条同作者陈述，尚未可靠关联到合格路线 |
| Q3 时长 | UNSUPPORTED |
| Q4 交通/限制 | UNSUPPORTED |

剩余缺口：路线、体验差异、时长、交通、不同来源对照、主题多样性；图片未分析、PARTIAL_TEXT、正文两种表示的重叠关系仍有既有不确定性。正文明确提到 2025 国庆，但机器结构化字段没有确定到日的旅行日期，不能用 2026 发布日期替代。新增五天/不自驾后保留 DAYS_FIT 和 NON_SELF_DRIVE；没有抹去自驾条件。

本轮可读局部线索（仅概述这 3 条已入库陈述及其条件，不是路线方案）：

1. **甘白路、亚青寺是作者个人的兴趣重点。** C7 的原文上下文对应作者 2025 国庆自驾，涉及白玉到亚青寺这一段；不能推出当前道路、开放状况或五天安排可行。
2. **作者对亚青寺给出了与色达相关的主观比较。** C8 只说明作者个人感受，不证明景区规模、排名、普遍优劣或免预约政策。
3. **德格印经院仅保留为作者有条件的建议。** C5 的条件明确记载作者本人未实际游览，涉及时间与停车限制；不写成已验证体验或推荐必去点。

以上 3 条 Work 概述可以逐项回查已接纳证据及条件，但没有形成程序路线方向（candidate_direction_count=0），没有足够时长或非自驾交通证据。不能仅因没有发布不支持结论就让 G1 通过。当前模型偏向截取评价片段，实际原文的路线/日程信息未被充分转为合格研究证据；本轮按要求没有改提示词来追求更好输出。

## 测试、测量与安全

- 全量 `python -m pytest -q --tb=short`：**859 passed，2 条既有依赖弃用警告**，23.21 秒。沿用 849 项，新增 10 项有界回归。
- 本地 HTTP 测试涵盖：延迟超过旧缩放阈值但低于新阈值；HTTP200 后正文停滞；连接/响应头前失败；持续空白直到总截止；子进程提前退出；已提交结果不被截止覆盖；并发启动仅一次派发；120 秒配置真实传到 opener/子进程。v7 的已审核非空 Evidence 迁移到 v8 保持不变。
- Ruff PASS；Mypy PASS（44 个源文件）；文档/契约 12/12 PASS（47 个领域定义）；git diff --check PASS。未新增真实合成模型连通性测试。
- 新增 XHS connect/search/detail=**0/0/0**，browser/sidecar 启动=0/0，模型提取=**1**，连接测试/模型审稿/自动重试=0/0/0。真实 CLI 限定 DeepSeek，审核与恢复完全禁网；XHS/Playwright 模块未导入。
- HTTP 派发数、响应状态、完整响应字节、时间点均来自实际应用层观测；全主机网络请求、完整线缆传输字节、服务端内部行为和计费 NOT_MEASURED/UNKNOWN。没有统计保活次数。
- 运行库及本轮安全 JSON 未发现配置密钥；安全 JSON 无真实正文片段。候选及条件只留在受管私人审计表；合成凭据 sentinel 未进入诊断/数据库/输出。
- 完整待推送范围包含上轮未推送提交，按明确文件清单扫描凭据、运行目录、二进制及真实正文连续 32 字片段。无原文、SQLite、profile、图片、响应包进入 Git；不把扫描通过当成完整隐私证明。
- 原快照、profile 保留，未 disconnect、未清缓存。结束时只读进程核对：本轮模型/探针/测试辅助进程 0，专用 profile 浏览器进程 0。

## Git 与停止

本轮第一次默认只读 ls-remote 成功，远端分支为 `62bb3aa5ab87b960712d396e1676bfbe394e87e7`，属于本地祖先；不再执行 HTTP/1.1 对照。GitHub 连接恢复不解释 DeepSeek 的历史超时。按本任务“一次正常推送”的范围，收尾将此前两次本地提交与本轮修复/报告一起推送；未关闭证书验证，未换远端、强推或合并 master。

代码提交 [c167db2f0edb68136e82940d1afd8d6ce20edec1](https://github.com/wenbox98/TravelAgent/commit/c167db2f0edb68136e82940d1afd8d6ce20edec1)：18 个文件，742 行新增、30 行删除。报告与 README/验收状态另提交；最终报告提交和远端完整 SHA 的核对结果在交付回复中列出，避免报告自引用尚未生成的提交值。三份原有未跟踪 T03 报告继续保留。

本轮完成后停止。下一步应根据局部材料的研究相关性和剩余 Coverage 单独决定；本轮不改提示词、不抓第 2/3 篇、不再调用模型、不开展其他功能。
