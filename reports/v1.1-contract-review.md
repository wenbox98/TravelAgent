# v1.1 文档与契约审查

审查日期：2026-09-22。范围：CODEX_START 指定清单、T00/T01 输入、Schema、OpenAPI、SQLite、defaults、合成夹具。只审查本地规格；未验证候选上游提交或真实站点。

| 差异 | 实际发现 | 本轮处理 |
|---|---|---|
| 预算表互相冲突 | 04 章为首轮 2/4、补查 1/3、累计 4/10；defaults 为 3/6、2/4、5/10 | 统一采用现有 v1.1 defaults，文档写明轮次与累计关系，不重置 revision 预算；调度执行留待 T04 |
| Evidence 最低字段不完整 | Schema 缺 destination、applicable_conditions、source_type、source_title、confidence；概念名称与已有字段不同 | 扩展 bundle/claim，保留已有名称并在 PoC 设计逐项映射；同步 fixtures、OpenAPI 版本、v2 SQL 和测试 |
| 登录/验证错误名称冲突 | PoC 用 NEED_LOGIN/VERIFICATION_REQUIRED，FetchResult/Overview 用 AUTH_REQUIRED/CHALLENGE | 应用统一新名称；03 章保留上游概念并在 07 章明确 adapter 映射，不等同 HTTP 401 |
| SQLite 不能约束正文完整度 | v1 sources.completeness 是任意 TEXT；L0 示例使用 FULL | v1 作为历史迁移基线保留；v2 显式转换四种已知旧值，新增写入/修改触发器；未知历史值导致升级回滚；L0 示例更新 FULL_TEXT |
| ResearchSession 无领域模型 | 任务要求该模型，原 Schema 只有 SQL 表 | 增加严格定义，预算/已用额度分开，数据保持跨重启，未实现 T04 自动预算调度 |
| UNKNOWN 用途仍能勾选持久化 | Schema 只约束 inference/embed 依赖，没有限制 UNKNOWN 持久化 | Schema 增加限制；仓储检查审核时间、有效期、账号、版本，并拒绝隐式来源覆盖 |
| 观测要求未完全落到契约 | PoC 的 research_id/search_operations/detail_operations 与现有字段名称不同；stop_reason/insufficient_evidence 尚不存在 | 明确命名映射；后两者和选择审计属于 T04 的显式契约任务，本轮未绕过 additionalProperties 塞字段 |
| 侧车策略措辞 | 03 章称维护有限 patch，PoC 分析要求 wrapper 优先 | 采用 wrapper 优先，wrapper 无法隔离路由/日志/取消时再做最小 patch；T02 需真实源码与构建证据 |
| 任务验收 ID 范围不一致 | T00 引用 REL01（实为 T11/L5）、T01 引用 PLAN05（实为 T07 预算计算） | 不冒充这些用例通过；本轮完成 DATA01～03 范围，SEC01 仅 API 子范围；REL01、PLAN05 应用实现仍 SKIPPED |
| 文档校验工具误扫描依赖 | 安装工程依赖后 rglob 会扫描 node_modules/venv 的 JSON 与 Markdown | 限定为项目自有文档/契约/夹具目录；保留实际校验能力 |
| 原包历史状态描述 | README/报告生成器称无应用测试；MANIFEST 描述原始包 | README 更新为当前状态；L0 报告明确只代表自身范围；MANIFEST 保留为原包导入清单，不视为当前构建清单 |

本次没有 Git 仓库，且未获提交要求，因此没有“contract-change commit”。本报告与 T01 报告作为可审查的显式契约变更记录，未静默选择不兼容字段。

四种完整度仅说明取得内容的范围；SUMMARY_ONLY 不升级 FULL_TEXT。置信度未知为 null，元数据不能生成正文结论。mock 来源为合成，不代表真实 API 字段或站点成功。

仍待 T02～T05：只读侧车构建、正常登录与恢复、Evidence Gap 循环、3/6 调度门禁、source 单次详情、增量补查、revision 防覆盖、筛选审计、RAG/删除传播。全部 XPOC01～10/SEL01～12 保持未验收，不能用 T01 fixtures 替代。
